name: Generate Snake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch: {}
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake game from GitHub contribution grid
        uses: Platane/snk@v3
        with:
          github_user_name: Berkanksz
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#00F0FF
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#00F0FF
            dist/ocean.gif?color_snake=#00F0FF&color_dots=00F0FF,0D1117,161b22

      - name: Push generated files
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
